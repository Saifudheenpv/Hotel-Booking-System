spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_booking_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Shanu@9090!
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  
  sql:
    init:
      mode: never

logging:
  level:
    com.hotel: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.web: INFO

app:
  data:
    initialize: true